"""RAG experiment harnesses (retrieval quality, downstream impact).

Production RAG code lives in the top-level ``rag`` package; this subpackage
holds only the experiment/eval harnesses that import it.
"""
